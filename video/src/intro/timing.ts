export const FPS = 30;

// Seconds into track.mp3 where frame 0 lands. Tuned for the previous ~18s
// beat-synced cut (it ended as the track's energy collapsed at ~90s); the
// 30s storyboard cut needs re-tuning against the licensed audio slice.
export const TRACK_OFFSET_SEC = 70.57;
